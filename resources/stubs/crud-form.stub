@php
    $controller = app('{{controllerNamespace}}');
    $fields = $controller->getFormFields();
    $item = $item ?? null;
@endphp

<x-crud.form
    :method="$method ?? 'create'"
    :item="$item"
    :fields="$fields"
    form-id="{{formId}}"
/>
